To run the Project, type this command in git bash:
npx playwright install

To see the Alure Report, run this command in git bash (you should have Java in your device installed):
	npx allure open allure-report

If you do not have Java, run this command:
npx playwright show-report reports/html-report
